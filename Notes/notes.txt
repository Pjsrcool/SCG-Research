
Enable live chromium replay:
when exporting a puppeteeer script from Google Chrome,
update the browser launch parameter to include:

const browser = await puppeteer.launch({ headless: false })

otherwise, the browser will not launch and show a live
replay. default {headless : true}, which runs the replay
int he background

------------

Prevent automatic open of browser:
By default, node automatically opens the default browser
when running a project. To prevent this behavior,
create a file called ".env" in project root directory.
in .env, add:

BROSWER=none

-------------

Babel:
install babel per project and make "es5" branch from main/master:
git checkout -b es5
npm install --save-dev @babel/core @babel/cli @babel/preset-env @babel/node

install the jsx syntax plugin as needed:
npm install --save-dev @babel/plugin-syntax-jsx

most projects that use react need the jsx syntax plugin anyway:
npm install --save-dev @babel/core @babel/cli @babel/preset-env @babel/node @babel/plugin-syntax-jsx

create .babelrc with the following content:

{
    "presets" : ["@babel/preset-env"],
    "plugins" : ["@babel/plugin-syntax-jsx"]    // as needed
}

inside package.json, add this script:
"es5": "babel src -d src_ES5",

run with:
npm run es5

-----------------